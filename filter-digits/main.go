package main

import "fmt"

func filterdigits(num []int){
	for i := range num{
		if i = 1 || 3{
			if i+1 = 2, 4, 6, 8{
				num = len(num)-num[i]int
			}
		}
	}
	return
}

func main(){
	num := []int
	fmt.Println("Enter numbers: ")
	fmt.Scan(&num)
	return num
	fmt.Println(filterdigits()) 
}